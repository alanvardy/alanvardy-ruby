<% provide(:title, "Contact Me") %>

<div class="content">
    <section>
    <h1>Contact Me</h1>
    <div class="row d-flex justify-content-center">
    <div class="col-12 col-lg-6">
        <p>You can use the form provided or email me at <a href="mailto:alan@alanvardy.com">alan@alanvardy.com</a></p>
        <p>I'm looking forward to hearing from you!</p>

    </div>
    <div class="col-12 col-lg-6">

        <%= form_for @message, url: messages_create_path do |f| %>
            <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, class: 'form-control'%>
            </div>

            <div class="form-group">
            <%= f.label :email %>
            <%= f.email_field :email, class: 'form-control'%>
            </div>

            <div class="form-group">
            <%= f.label :phone_number %>
            <%= f.telephone_field :phone_number, class: 'form-control'%>
            </div>

            <div class="form-group">
            <%= f.label :body %>
            <%= f.text_area :body, class: 'form-control', rows: '5'%>
            </div>

            <div class="form-group">
            <%= f.submit 'Send Form', class: 'btn btn-outline-dark'%>
            </div>
        <% end %>
    </div>
    </div>
    </section>
</div>